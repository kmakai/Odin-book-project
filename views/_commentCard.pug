mixin commentCard(comment)
  .comment-card 
    p.comment-text= comment.text 
    - const date = new Date(comment.createdAt)
    p.commentor= `${comment.user.name} - ${new Intl.DateTimeFormat('en-US',{month: 'short', day: "numeric", year: "numeric"}).format(date)}`